; Compile the opt-in transport without embedding a shared key.
[env:esp32_dev_ble_control]
extends = env:esp32_dev
build_flags =
    ${env:esp32_dev.build_flags}
    -DIOTSMARTSYS_BLE_COMMAND_ENABLED=1
